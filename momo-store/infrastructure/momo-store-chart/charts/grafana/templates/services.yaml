apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}"
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: {{ .Values.port }}
      protocol: TCP
      targetPort: http-grafana
  selector:
    app: "{{ .Values.name }}"
  sessionAffinity: None
  type: ClusterIP
